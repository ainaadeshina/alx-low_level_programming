# include "main.h"
/**
 * _abs - Calculates the absolute value of an integer.
 * @n: The integer to be calculated.
 * Return: The absolute of the given integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
